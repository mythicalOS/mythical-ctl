# Release staging (built by .github/workflows/release.yml)
dist/

# never publish internal planning / orchestration artifacts to this repo
.superpowers/
docs/superpowers/
.claude/

# Local secrets — never commit
.env
.env.*

# Editor / OS noise
.DS_Store
*.swp
